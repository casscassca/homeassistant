# Home Assistant configuration

This repository contains the version-controlled, non-secret portion of the
Home Assistant configuration.

Secrets, credentials, runtime state, databases, logs, and generated media stay
on the Home Assistant host and are excluded by `.gitignore`.

The deployment workflow will be added after the initial configuration is
reviewed and the GitHub Actions runner is configured on the Raspberry Pi.
