services:
  homeassistant:
    container_name: homeassistant
    image: ghcr.io/home-assistant/home-assistant:stable
    privileged: true
    network_mode: host
    volumes:
      - /home/cassandrameijers/homeassistant:/config
    restart: unless-stopped
