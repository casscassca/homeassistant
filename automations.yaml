- actions:
  - action: script.notify_security_snapshot
    data:
      camera_id: camera.backyard_camera
      snapshot_prefix: backyard
      title: Person Spotted - Backyard
      message: Someone was seen on the backyard camera
      view_path: /
  alias: Nest Backyard - Person Detected
  conditions:
  - condition: template
    value_template: '{{ trigger.event.data.new_state.attributes.event_type == ''camera_person''
      }}'
  - condition: not
    conditions:
    - condition: state
      entity_id: person.cass
      state: home
    - condition: state
      entity_id: person.jason
      state: home
  - condition: state
    entity_id: input_boolean.vacation_mode
    state: 'off'
  - condition: template
    value_template: '{{ this.attributes.last_triggered is none or (as_timestamp(now())
      - as_timestamp(this.attributes.last_triggered)) > 120 }}'
  description: ''
  id: '1782569285600'
  mode: single
  triggers:
  - event_data:
      entity_id: event.backyard_camera_motion
    event_type: state_changed
    trigger: event
- actions:
  - action: script.notify_security_snapshot
    data:
      camera_id: camera.driveway
      snapshot_prefix: driveway
      title: Person Spotted - Driveway
      message: Someone was seen on the driveway camera
      view_path: /
  alias: Nest Driveway - Person Detected
  conditions:
  - condition: template
    value_template: '{{ trigger.event.data.new_state.attributes.event_type == ''camera_person''
      }}'
  - condition: not
    conditions:
    - condition: state
      entity_id: person.cass
      state: home
    - condition: state
      entity_id: person.jason
      state: home
  - condition: state
    entity_id: input_boolean.vacation_mode
    state: 'off'
  - condition: template
    value_template: '{{ this.attributes.last_triggered is none or (as_timestamp(now())
      - as_timestamp(this.attributes.last_triggered)) > 120 }}'
  description: ''
  id: '1782569302342'
  mode: single
  triggers:
  - event_data:
      entity_id: event.driveway_motion
    event_type: state_changed
    trigger: event
- actions:
  - action: script.notify_security_snapshot
    data:
      camera_id: camera.front_yard_camera
      snapshot_prefix: front_yard
      title: Person Spotted - Front Yard
      message: Someone was seen on the front yard camera
      view_path: /front-yard-view/default_view
  alias: Nest Frontyard - Person Detected
  conditions:
  - condition: template
    value_template: '{{ trigger.event.data.new_state.attributes.event_type == ''camera_person''
      }}'
  - condition: not
    conditions:
    - condition: state
      entity_id: person.cass
      state: home
    - condition: state
      entity_id: person.jason
      state: home
  - condition: state
    entity_id: input_boolean.vacation_mode
    state: 'off'
  - condition: template
    value_template: '{{ this.attributes.last_triggered is none or (as_timestamp(now())
      - as_timestamp(this.attributes.last_triggered)) > 120 }}'
  description: ''
  id: '1782569371087'
  mode: single
  triggers:
  - event_data:
      entity_id: event.front_yard_camera_motion
    event_type: state_changed
    trigger: event
- actions:
  - action: script.notify_security_snapshot
    data:
      camera_id: camera.gate_bins
      snapshot_prefix: gate_bins
      title: Person Spotted - Gate & Bins
      message: Someone was seen at the gate & bins camera
      view_path: /
  alias: Nest Gate & Bins - Person Detected
  conditions:
  - condition: template
    value_template: '{{ trigger.event.data.new_state.attributes.event_type == ''camera_person''
      }}'
  - condition: not
    conditions:
    - condition: state
      entity_id: person.cass
      state: home
    - condition: state
      entity_id: person.jason
      state: home
  - condition: state
    entity_id: input_boolean.vacation_mode
    state: 'off'
  - condition: template
    value_template: '{{ this.attributes.last_triggered is none or (as_timestamp(now())
      - as_timestamp(this.attributes.last_triggered)) > 120 }}'
  description: ''
  id: '1782569824728'
  mode: single
  triggers:
  - event_data:
      entity_id: event.gate_bins_motion
    event_type: state_changed
    trigger: event
- actions:
  - action: script.notify_security_snapshot
    data:
      camera_id: camera.patio_camera
      snapshot_prefix: patio
      title: Person Spotted - Patio
      message: Someone was seen on the patio camera
      view_path: /
  alias: Nest Patio - Person Detected
  conditions:
  - condition: template
    value_template: '{{ trigger.event.data.new_state.attributes.event_type == ''camera_person''
      }}'
  - condition: not
    conditions:
    - condition: state
      entity_id: person.cass
      state: home
    - condition: state
      entity_id: person.jason
      state: home
  - condition: state
    entity_id: input_boolean.vacation_mode
    state: 'off'
  - condition: template
    value_template: '{{ this.attributes.last_triggered is none or (as_timestamp(now())
      - as_timestamp(this.attributes.last_triggered)) > 120 }}'
  description: ''
  id: '1782569851032'
  mode: single
  triggers:
  - event_data:
      entity_id: event.patio_camera_motion
    event_type: state_changed
    trigger: event
- actions:
  - action: light.toggle
    target:
      entity_id: light.cass_bedside
  alias: Cass Bedside Button - Light Toggle
  conditions: []
  description: ''
  id: cass_bedside_button_light
  mode: single
  triggers:
  - entity_id: sensor.cass_bedside_button_action
    to: single
    trigger: state
- actions:
  - action: light.toggle
    target:
      entity_id: light.jason_s_bedside
  alias: Jason Bedside Button - Light Toggle
  conditions: []
  description: ''
  id: jason_bedside_button_light
  mode: single
  triggers:
  - entity_id: sensor.jason_bedside_button_action
    to: single
    trigger: state
- id: reolink_front_door_activity
  alias: Reolink Front Door - Activity Detected
  description: ''
  triggers:
  - entity_id:
    - binary_sensor.front_door_person
    - binary_sensor.front_door_vehicle
    - binary_sensor.front_door_visitor
    - binary_sensor.front_door_pet
    to: 'on'
    trigger: state
  conditions:
  - condition: template
    value_template: '{{ (as_timestamp(now()) - as_timestamp(states.binary_sensor.front_door_contact.last_changed))
      > 30 }}'
  - condition: template
    value_template: '{{ (as_timestamp(now()) - as_timestamp(states.cover.ratgdo_door.last_changed))
      > 120 }}'
  - condition: template
    value_template: '{{ this.attributes.last_triggered is none or (as_timestamp(now())
      - as_timestamp(this.attributes.last_triggered)) > 120 }}'
  actions:
  - action: script.notify_security_snapshot
    data:
      camera_id: camera.front_door_snapshots_fluent
      snapshot_prefix: front_door
      title: Front Door - {{ trigger.to_state.name }}
      message: Detected at the front door.
      view_path: /front-yard-view/default_view
  max_exceeded: silent
  mode: single
- id: petlibro_cleaning_reset_after_sweepy
  alias: Reset Petlibro cleaning state after Clean setup
  description: ''
  triggers:
  - trigger: state
    entity_id: sensor.juicebox_sweepy_clean_setup_last_done
  conditions:
  - condition: template
    value_template: >-
      {{ trigger.from_state is not none
         and trigger.to_state is not none
         and trigger.from_state.state not in ['unknown', 'unavailable']
         and trigger.to_state.state not in ['unknown', 'unavailable']
         and trigger.from_state.state != trigger.to_state.state }}
  actions:
  - action: rest_command.petlibro_login
    response_variable: petlibro_login
  - variables:
      petlibro_login_status: "{{ petlibro_login.status | default('unknown') }}"
      petlibro_login_code: >-
        {% set raw = petlibro_login.content %}
        {% if raw is mapping %}
          {{ raw.get('code', 'not returned') }}
        {% elif raw is string %}
          {{ (raw | from_json).get('code', 'not returned') }}
        {% else %}
          not returned
        {% endif %}
      petlibro_token: >-
        {% set raw = petlibro_login.content %}
        {% if raw is mapping %}
          {{ (raw.get('data') or {}).get('token', '') }}
        {% elif raw is string %}
          {{ ((raw | from_json).get('data') or {}).get('token', '') }}
        {% else %}
        {% endif %}
  - choose:
    - conditions:
      - condition: template
        value_template: >-
          {{ petlibro_login_status | int(0) >= 200
             and petlibro_login_status | int(0) < 300
             and petlibro_login_code | string == '0'
             and petlibro_token | length > 0 }}
      sequence:
      - action: rest_command.petlibro_cleaning_reset
        data:
          token: "{{ petlibro_token }}"
          device_sn: "{{ states('input_text.petlibro_wet_food_device_sn') }}"
          request_id: "{{ context.id | replace('-', '') }}"
        response_variable: petlibro_response
      - variables:
          petlibro_api_status: "{{ petlibro_response.status | default('unknown') }}"
          petlibro_api_code: >-
            {% set raw = petlibro_response.content %}
            {% if raw is mapping %}
              {{ raw.get('code', 'not returned') }}
            {% elif raw is string %}
              {{ (raw | from_json).get('code', 'not returned') }}
            {% else %}
              not returned
            {% endif %}
      - choose:
        - conditions:
          - condition: template
            value_template: >-
              {{ petlibro_api_status | int(0) >= 200
                 and petlibro_api_status | int(0) < 300
                 and petlibro_api_code | string == '0' }}
          sequence:
          - action: logbook.log
            data:
              name: Petlibro cleaning reset
              message: >-
                API reset returned HTTP {{ petlibro_api_status }}
                with API code {{ petlibro_api_code }}.
        default:
        - action: persistent_notification.create
          data:
            title: Petlibro cleaning reset failed
            message: >-
              API reset returned HTTP {{ petlibro_api_status }}
              with API code {{ petlibro_api_code }}.
    default:
    - action: persistent_notification.create
      data:
        title: Petlibro login failed
        message: >-
          Login returned HTTP {{ petlibro_login_status }}
          with API code {{ petlibro_login_code }}.
  mode: single
